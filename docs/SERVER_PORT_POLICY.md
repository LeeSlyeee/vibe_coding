# 🏥 Vibe Coding Server Port Policy

> **마지막 업데이트**: 2026-02-14
> **작성자**: Antigravity (AI Assistant)
> **목적**: 서버 포트 충돌 방지 및 명확한 역할 분담

## 1. 환자용 서비스 (Patient App / Maum-On)

- **역할**: 일반 사용자 감정 일기, 캘린더, AI 채팅
- **도메인**: `https://217.142.253.35.nip.io/`

| 컴포넌트     | 기술 스택  | 포트 (Port) | 로컬 주소        | 비고                    |
| :----------- | :--------- | :---------- | :--------------- | :---------------------- |
| **Backend**  | Flask      | **5000**    | `127.0.0.1:5000` | Nginx `/api` 라우팅     |
| **Frontend** | Vite (Vue) | **5173**    | `127.0.0.1:5173` | Nginx `/` (Root) 라우팅 |

## 2. 의료진용 서비스 (Medical Staff / InsightMind)

- **역할**: 병원 관리자 대시보드, 환자 모니터링
- **도메인**: `https://217.142.253.35.nip.io/staff`

| 컴포넌트     | 기술 스택    | 포트 (Port) | 로컬 주소        | 비고                   |
| :----------- | :----------- | :---------- | :--------------- | :--------------------- |
| **Backend**  | Django       | **8000**    | `127.0.0.1:8000` | Nginx `/api/v1` 라우팅 |
| **Frontend** | Vite (React) | **3000**    | `127.0.0.1:3000` | Nginx `/staff` 라우팅  |

## 3. 인프라 (Infrastructure)

| 서비스    | 역할          | 포트 (Port)   | 비고              |
| :-------- | :------------ | :------------ | :---------------- |
| **Nginx** | Reverse Proxy | **80 -> 443** | SSL 인증서 적용됨 |
| **Redis** | Cache/Queue   | **6379**      | Celery Broker     |

---

## ⚠️ 주의사항

1. **포트 변경 금지**: 위 포트는 `nginx_backend.conf` 및 프로젝트 코드에 하드코딩된 표준입니다. 임의 변경 시 전체 장애 발생.
2. **실행 명령어**:
   - Flask: `nohup python app.py > backend.log 2>&1 &` (Port 5000 자동)
   - Django: `nohup python manage.py runserver 0.0.0.0:8000 > django.log 2>&1 &`
   - Vite (Patient): `nohup npm run dev -- --host --port 5173 > frontend.log 2>&1 &`
   - Vite (Staff): `nohup npm run dev -- --host --port 3000 > staff_frontend.log 2>&1 &`
